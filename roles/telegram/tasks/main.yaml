- name: packages
  ansible.builtin.apt:
    pkg:
      - python-pip

- name: pip
  ansible.builtin.pip:
    name: pip
    state: latest

- name: virtualenv
  ansible.builtin.pip:
    name: virtualenv
    version: 20.4.7
